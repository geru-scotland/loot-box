const shared = {
    INVENTORY_CAPACITY: 20
}

const roles = {
    USER: 'user',
    ADMIN: 'admin'
}

const extensions = {
    PNG: ".png",
    JPEG: ".jpeg"
}

module.exports = { shared, roles, extensions }