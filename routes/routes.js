const routes = {
    auth: {
        register: '/auth/register',
        login: '/auth/login',
        logout: '/auth/logout'
    },
    admin: {
        panel: '/admin/'
    }
};

module.exports = routes;
