<!DOCTYPE html>
<html>
<head>
    <title><%= literals.auth.REGISTER_TITLE %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="stylesheet" href="/stylesheets/forms.css" />
</head>
<body>
<div class="container">
    <h1 class="title"><%= literals.auth.REGISTER_HEADER %></h1>
    <% if (typeof error !== 'undefined' && error) { %>
        <p class="error-message"><%= error %></p>
    <% } %>
    <form action="<%= routes.auth.register %>" method="POST">
        <input type="text" name="username" placeholder="<%= literals.auth.REG_PLH_UNAME %>" required />
        <input type="email" name="email" placeholder="<%= literals.auth.REG_PLH_EMAIL %>" required />
        <input type="password" name="password" placeholder="<%= literals.auth.REG_PLH_PASS %>" required />
        <button type="submit">Register</button>
    </form>
    <p><a href="<%= routes.auth.login %>" class="link"><%= literals.auth.ALREADY_ACCOUNT %></a></p>
</div>
</body>
</html>
