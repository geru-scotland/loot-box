const errors = {
    auth: {
        WRONG_CREDENTIALS: 'Incorrect credentials',
        USERNAME_NOTFOUND: "That username doesn't exist",
        USERNAME_ALREADY_EXISTS: 'Username already exists!'
    }
};

module.exports = { errors }